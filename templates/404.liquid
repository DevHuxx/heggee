{% comment %} By Shopify Partner ak44sh - High-Performance Brutalist Theme {% endcomment %}
<div class="container" style="padding-top:calc(2rem + 6rem);padding-bottom:4rem;text-align:center;">
  <h1 style="font-size:var(--text-6xl);margin-bottom:1rem;">{{ 'general.404.title' | t }}</h1>
  <p style="color:var(--color-muted-fg);margin-bottom:2rem;">{{ 'general.404.subtext' | t }}</p>
  <a href="{{ routes.root_url }}" class="btn-primary">Return Home</a>
</div>
